import { DefaultCoverUrlDirective } from './default-cover-url.directive';

describe('DefaultCoverUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultCoverUrlDirective();
    expect(directive).toBeTruthy();
  });
});
