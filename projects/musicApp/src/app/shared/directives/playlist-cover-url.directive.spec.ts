import { PlaylistCoverUrlDirective } from './playlist-cover-url.directive';

describe('PlaylistCoverUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaylistCoverUrlDirective();
    expect(directive).toBeTruthy();
  });
});
