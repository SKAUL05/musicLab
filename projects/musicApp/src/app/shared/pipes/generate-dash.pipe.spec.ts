import { GenerateDashPipe } from './generate-dash.pipe';

describe('GenerateDashPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateDashPipe();
    expect(pipe).toBeTruthy();
  });
});
